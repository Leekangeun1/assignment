The syntax for matrix.sh

-> ./matrix.sh (int) (int)
ex) ./matrix.sh 3 4

The two parameters must be declared as integers over 0.
If any integer under 1 is declared, "invalid parameter" sign will printed out.
